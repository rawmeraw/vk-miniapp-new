# Changelog - VK Мини-приложение "Живое!"

## Версия 2.0 - Исправления и улучшения

### 🖼️ Изображения
- ✅ **ИСПРАВЛЕНО**: Изображения теперь загружаются из API
- ✅ Поддержка `main_image` и `small_pic` полей
- ✅ Оптимизация размера изображений (300px)
- ✅ Правильная обработка заглушек

### 🎫 Кнопки покупки билетов
- ✅ **НОВОЕ**: Крупные кнопки "Купить билет от XXX₽"
- ✅ Отображение цены в кнопке
- ✅ Градиентный дизайн с анимацией hover
- ✅ Отдельные от основной ссылки карточки

### 🏷️ Цветные теги
- ✅ **НОВОЕ**: Цветовая схема по категориям:
  - **Live** - красный фон (#fce8e6)
  - **Pop** - синий фон (#e8f0fe) 
  - **Classic** - зеленый фон (#e8f5e8)
- ✅ Автоматическое определение категории из API
- ✅ Fallback по названию тега

### 🔤 Шрифты
- ✅ **ОБНОВЛЕНО**: Подключены фирменные шрифты:
  - **Jost** - основной шрифт
  - **Jost Light** - для подзаголовков
  - **IBM Plex Serif** - для акцентов
- ✅ Применены ко всем элементам интерфейса

### 📅 Календарь
- ✅ **ИСПРАВЛЕНО**: Навигация по датам работает корректно
- ✅ **ИСПРАВЛЕНО**: Показ только будущих дат (включая сегодня)
- ✅ **ИСПРАВЛЕНО**: Правильная фильтрация концертов по выбранной дате
- ✅ Улучшенная логика переключения месяцев

### 🗺️ Карта
- ✅ **НОВОЕ**: Показ только сегодняшних концертов
- ✅ **НОВОЕ**: Время и название концерта в подсказках маркеров
- ✅ Улучшенные информационные окна
- ✅ Группировка по площадкам

### 🎨 Дизайн
- ✅ Улучшенная структура карточек концертов
- ✅ Правильное позиционирование кнопок
- ✅ Консистентная типографика
- ✅ Улучшенные состояния hover и focus

## Технические детали

### API Integration
```javascript
// Поддержка обоих полей изображений
let imageUrl = concert.main_image || concert.small_pic || 'zhivoe_logo.jpg';

// Оптимизация размера
if (imageUrl.includes('permlive.ru') && !imageUrl.includes('zhivoe_logo.jpg')) {
    imageUrl += '?w=300&h=300&fit=crop';
}
```

### Цветовые схемы тегов
```javascript
// Определение цвета по категории
if (concert.tag_categories && concert.tag_categories.length > 0) {
    const category = concert.tag_categories[0].toLowerCase();
    if (category === 'live') tagClass += ' live';
    else if (category === 'pop') tagClass += ' pop';
    else if (category === 'classic') tagClass += ' classic';
}
```

### Календарь - только будущие даты
```javascript
// Показываем только будущие даты (включая сегодня)
if (d >= new Date(today.getFullYear(), today.getMonth(), today.getDate())) {
    dates.push(new Date(d));
}
```

### Карта - только сегодня
```javascript
// Фильтруем только сегодняшние концерты
const today = new Date().toISOString().split('T')[0];
const todayConcerts = this.filteredConcerts.filter(concert => concert.date === today);
```

## Следующие шаги

1. Тестирование всех функций
2. Проверка на мобильных устройствах
3. Развертывание на GitHub Pages
4. Создание VK мини-приложения
5. Отправка на модерацию